import React, { Component } from "react";

export default class TopBar extends Component {
  constructor(){
    
  }

  render() {
    return (  
      //Icon/Logo
      //Right Side icon(use menu) 
    );
  }
}
