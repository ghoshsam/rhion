// var express = require("express");
// var path = require("path");

//let app = express();
require("bootstrap");
import "./style.scss";
if (module.hot) {
  module.hot.accept();
}
// app.get("/", (req, res) => {
//   res.sendFile(path.join(__dirname + "/index.html"));
// });

// app.listen(3001);
